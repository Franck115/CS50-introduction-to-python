# ask user for input
exp = input("Expression: ")


#change to float

x = float(exp.strip()[0:2])
y = exp.strip()[2:3]
z = float(exp.strip()[4:6])

# calculate the expression
if y == "+":
    result = x + z
elif y == "-":
    result = x - z
elif y == "*":
    result = x * z
else:
    result = x / z

print(result)
